# Weather App

## This App shows the weather, humidity and windspeed of an any entered city using forward geo-coding that fetches the latitude and longitude of the entered city via an API. This data is then passed on to another API, which shows the weather of the entered city by fetching the data using latitudes and longitudes only.